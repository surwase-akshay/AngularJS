



function validate(email, password) {
    let status = false;

    if (email === "akshay" && password === "akshay") {
        status = true;
    }

    return status;  // Return the status correctly
}

